# Infrastructure (Azure IaC)

This directory contains Infrastructure as Code files written in **Bicep**.

## Resources created
- Azure Resource Group
- Azure Container Registry (ACR)

## Purpose
Infrastructure is provided only for demonstration and registry usage.
The application itself is **not deployed to Azure**.

## Files
- `main.bicep` – main infrastructure definition
- `parameters.json` – deployment parameters
